terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 4.0"
    }
  }
}

provider "aws" {
  region = var.region_name
}

default tags {
  "Application" = "Kubernetes"
  "Tool" = "terraform" 
}